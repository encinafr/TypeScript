function Calcular(): void{
    let num1 : number = parseInt((<HTMLInputElement> document.getElementById("txtNum1")).value);
    let num2 : number =  parseInt((<HTMLInputElement> document.getElementById("txtNum1")).value);


    let rdoButton : NodeListOf<HTMLElement>  = (document.getElementsByName("rdoOperacion"));

    for(let i = 0; i < rdoButton.length; i++)
    {
        if((any (rdoButton[i])
    }
   
        

}